# Copy to .env and fill in to run with --live. The offline (stub) path needs none of these.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
# Optional retrieval credentials (a tool is only offered at setup if its key is present)
GITHUB_TOKEN=
SEMANTIC_SCHOLAR_API_KEY=
