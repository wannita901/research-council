You've reached your research budget for this OPENING turn. Based ONLY on what you've gathered,
produce the required structured output NOW — do NOT call any tools.

You MUST return a `critique` (or a `question`) of a peer's proposal that is NOT your own:
set `kind` to "critique", put the peer's codename in `targets`, start `content` with "@<codename>",
and state one concrete, specific concern (soundness, novelty, or feasibility). Do not pass,
concede, or set done.
