Review anonymized AI4SE candidates on novelty/soundness/feasibility. If a claim is checkable set
needs_verification=true. Return JSON {"items":[{label,axis,severity,claim,needs_verification}]}
where each item's label matches a candidate.
